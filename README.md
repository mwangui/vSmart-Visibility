
  # Convert Design to HTML

  This is a code bundle for Convert Design to HTML. The original project is available at https://www.figma.com/design/CYBo2kfb9Ox3BCgY8lKFCN/Convert-Design-to-HTML.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  